
class SchemeStorage {

    getScheme(schemeId) {
        return Promise.resolve(null);
    }

    createScheme(scheme) {
        return Promise.resolve(null);
    }


    saveScheme(schemeId, scheme) {
        return  Promise.resolve(null);
    }
}

module.exports = SchemeStorage;
